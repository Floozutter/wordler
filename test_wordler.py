import wordler

def h(*args: int) -> tuple[wordler.Hint, ...]:
    return tuple(map(wordler.Hint, args))

def test_deduce_hints() -> None:
    assert wordler.deduce_hints("", "") == h()
    assert wordler.deduce_hints("A", "Z") == h(0)
    assert wordler.deduce_hints("A", "A") == h(2)
    assert wordler.deduce_hints("AB", "CC") == h(0, 0)
    assert wordler.deduce_hints("AB", "AB") == h(2, 2)
    assert wordler.deduce_hints("AB", "BA") == h(1, 1)
    assert wordler.deduce_hints("AB", "AA") == h(2, 0)
    assert wordler.deduce_hints("AB", "BB") == h(0, 2)
    assert wordler.deduce_hints("AB", "CA") == h(0, 1)
    assert wordler.deduce_hints("AB", "BC") == h(1, 0)
    assert wordler.deduce_hints("AAAAA", "ZZZZZ") == h(0, 0, 0, 0, 0)
    assert wordler.deduce_hints("ABCDE", "ZYXWV") == h(0, 0, 0, 0, 0)
    assert wordler.deduce_hints("AAAAA", "AAAAA") == h(2, 2, 2, 2, 2)
    assert wordler.deduce_hints("ABCDE", "ABCDE") == h(2, 2, 2, 2, 2)
    assert wordler.deduce_hints("AABAA", "BBBBB") == h(0, 0, 2, 0, 0)
    assert wordler.deduce_hints("AABAA", "AAAAA") == h(2, 2, 0, 2, 2)
    assert wordler.deduce_hints("ABBBB", "CCCCA") == h(0, 0, 0, 0, 1)
    assert wordler.deduce_hints("ABBBB", "CCACA") == h(0, 0, 1, 0, 0)
    assert wordler.deduce_hints("ABBBB", "ACACA") == h(2, 0, 0, 0, 0)
    assert wordler.deduce_hints("ABBBB", "BBBBA") == h(1, 2, 2, 2, 1)
    assert wordler.deduce_hints("AAABB", "BBBAA") == h(1, 1, 0, 1, 1)
    assert wordler.deduce_hints("BUNNY", "NANNY") == h(0, 0, 2, 2, 2)
    assert wordler.deduce_hints("BUNNY", "INNER") == h(0, 1, 2, 0, 0)
    assert wordler.deduce_hints("BUNNY", "ENNUI") == h(0, 1, 2, 1, 0)
    assert wordler.deduce_hints("BUNNY", "SUNNY") == h(0, 2, 2, 2, 2)
    assert wordler.deduce_hints("BUNNY", "BNNNY") == h(2, 0, 2, 2, 2)
    assert wordler.deduce_hints("BUNNY", "BNUUY") == h(2, 1, 1, 0, 2)
    assert wordler.deduce_hints("BUNNY", "BUNNY") == h(2, 2, 2, 2, 2)
